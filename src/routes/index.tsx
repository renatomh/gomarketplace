import React from 'react';

import AppRoutes from './app.routes';

// Centralizando as rotas para o aplicativo e disponibilizando-as
const Routes: React.FC = () => {
  return <AppRoutes />;
};

export default Routes;
